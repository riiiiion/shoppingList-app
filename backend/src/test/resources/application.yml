spring:
  profiles:
    active: test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:digbase-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=PostgreSQL;
  jpa:
    show-sql: true
  main:
    allow-bean-definition-overriding: on


logging:
  level:
    root: info

trinity:
  host:
    frontend: https://localhost:3000

